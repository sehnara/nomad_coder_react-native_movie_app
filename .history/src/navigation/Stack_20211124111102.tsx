import React from "react"
import {} from "react-native"
import {createNativeStackNavigator} from "@react-navigation/native-stack"
const stack = createNativeStackNavigator)_
const Stack = () => {
   return (
    
   )
}

export default Stack